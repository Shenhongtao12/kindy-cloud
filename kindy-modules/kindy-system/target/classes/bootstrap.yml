# Tomcat
server:
  port: 9201

# Spring
spring: 
  application:
    # 应用名称
    name: kindy-system
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: 591e813f-5187-4356-97e6-79fc7cf4aa48
      config:
        namespace: 591e813f-5187-4356-97e6-79fc7cf4aa48
        # 配置中心地址
        server-addr: 127.0.0.1:8848
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
